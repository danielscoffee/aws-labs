{
  "name": "cdk-lambda-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "synth": "npx cdk synth",
    "diff":  "npx cdk diff",
    "deploy": "npx cdk deploy --require-approval never",
    "destroy": "npx cdk destroy --force"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "aws-cdk": "^2.180.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.7.0"
  }
}
